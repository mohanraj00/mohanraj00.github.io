source "https://rubygems.org"
# Mirrors the GitHub Pages build environment so `make preview` renders
# exactly what a push will publish (remote_theme, feed, etc.).
gem "github-pages", group: :jekyll_plugins
