# mohanraj00.github.io

Mohan's blog: `/tech` (engineering writeups) and `/synapse` (Midweek Synapse syntheses).

Jekyll + minima, built by GitHub Pages on push; no local build needed. Posts are markdown in `_tech/` and `_synapse/` with `title` + `date` front matter. Voice, register, and formatting rules live in the Personal KB vault: `_meta/tech-writing/profile.md`, `_meta/linkedin-series/voice-profile.md`, `_meta/publishing-guidelines.md`.
